<!DOCTYPE html>
<html>
<head>
  <style media="screen">
    body {
      font-family: "Helvetica Neue",Arial,Helvetica,sans-serif;
      color: #333;
      background: #F0F0F0;
      line-height: 1.5;
      margin: 0 auto;
      text-align: center;
    }
    input[type=submit] {
      cursor: pointer;
      background-color: #2376D8;
      font-size: 1.2em;
      color: #fff;
      font-weight: 700;
      border-radius: 10px;
      margin: 1em;
      padding: .8em 2em;
    }
    .game {
      display: block;
      width: 50%;
      min-width: 40em;
      margin: 0 auto;
    }

    .label {
      color: #B00B1E;
      margin: 0 .5em;
    }

    .name {
      color: rebeccapurple;
    }

    .healthcontainer {
      background: #cce3f7;
      border-radius: 100px;
    }

    .healthcontainer .health {
      width: 0;
      height: 0.55em;
      border-radius: 100px;
      transition: all 0.3s;
      -moz-transition: all 0.3s;
      -webkit-transition: all 0.3s;
      -o-transition: all 0.3s;
    }

     .healthcontainer.veryweak .health {
      background: #e21213
    }

     .healthcontainer.weak .health {
      background: #FDA068
    }

     .healthcontainer.okay .health {
      background: #ffdb49
    }

     .healthcontainer.good .health {
      background: #9BF47D
    }

     .healthcontainer.strong .health {
      background: #76da69
    }
  </style>
</head>
<body>
<div>
It's the turn of the mighty <%= @game.next_attacker.name %>
</div>

<section class="game">
  <div name=player_1 style="width: calc(50% - 1.5em);float:left;margin: 0 .75em;">
    <h1><span class="label">Player 1:</span><span class="name"><%= @game.player1.name %></span></h1>
    <div><span class="name"><%= @game.player1.name %> </span><strong><%= @game.player1.health%>HP</strong></div>
    <div class="healthcontainer <%= @game.player1.health_status %>">
      <div class="health" style="width: <%= @game.player1.health %>%;"></div>
    </div>
  </div>

  <div name=player_2 style="width: calc(50% - 1.5em);float:right;margin: 0 .75em;">
    <h1><span class="label">Player 2:</span><span class="name"><%= @game.player2.name %></span> </h1>
    <div> <span class="name"><%= @game.player2.name %> </span><strong><%= @game.player2.health%>HP</strong></div>
    <div class="healthcontainer <%= @game.player2.health_status %>">
      <div class="health" style="width: <%= @game.player2.health %>%;"></div>
    </div>
  </div>
</section>
<div>
  <form action=/play/attack_p2 method=post>
    <input type=submit value=Attack>
  </form>
</div>

<% flash.keys.each do |type| %>
  <div data-alert class="flash <%= type %> alert-box radius">
    <%= flash[type] %>
    <a href="#" class="close">&times;</a>
  </div>
<% end %>
</body>
</html>
